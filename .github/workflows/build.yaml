name: Build and Distribute Flutter APKs
on:
  push:
    branches:
      - dev
jobs:
  build:
    if: contains(github.event.head_commit.message, 'build')
    runs-on: ubuntu-latest
    steps:
      - name: #! 1. التحقق من الكود
        uses: actions/checkout@v2

      - name: #! 2. إعداد JDK
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: #! 3. إعداد Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: stable

      - name: #! 4. تحميل التبعيات
        run: flutter pub get

      - name: #! 5. بناء نسخ الاندرويد
        run: flutter build apk --split-per-abi 
      
      - name: #! 6. تحميل النسخ كـ Artifacts
        uses: actions/upload-artifact@v3
        with:
           name: APKs
           path: |
             build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
             build\app\outputs\flutter-apk\app-arm64-v8a-release.apk
      #! 🚀 7. توزيع النسخ عبر Firebase
      - name: #! 🚀 armeabi-v7a
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.CREDENTIAL_FILE_CONTENT }}
          groups: armeabi-v7a
          file: build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
          releaseNotes: "armeabi-v7a"
     
      - name: #! 🚀 arm64-v8a
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.CREDENTIAL_FILE_CONTENT }}
          groups: arm64-v8a
          file: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk    
          releaseNotes: "arm64-v8a"
